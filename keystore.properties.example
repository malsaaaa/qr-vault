# Copy this file to keystore.properties (in the project root) and fill in the
# real values. keystore.properties is git-ignored and must never be committed.
storeFile=
storePassword=
keyAlias=qrvault
keyPassword=